# ==========================================
# DATABASE CONFIGURATION
# ==========================================

# Option 1: Remote Database (Recommended for Vercel/Production)
# Use this for TiDB Cloud, PlanetScale, Neon, or other MySQL cloud providers
# Format: mysql://username:password@host:port/database
DATABASE_URL="mysql://user:password@host:4000/database?ssl=true"

# Option 2: Local Database (Development Only)
# Remove or comment out DATABASE_URL above if using local database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=graky_store
DB_PORT=3306

# ==========================================
# NEXTAUTH CONFIGURATION
# ==========================================

# Required: Your app URL
# Development: http://localhost:3000
# Production: https://your-app.vercel.app
NEXTAUTH_URL=http://localhost:3000

# Required: Secret key for session encryption
# Generate with: openssl rand -base64 32
# MUST be different for development and production
NEXTAUTH_SECRET=your-secret-here-change-this

# ==========================================
# GOOGLE OAUTH (Optional)
# ==========================================

# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ==========================================
# PAYMENT GATEWAY (Optional)
# ==========================================

# HitPay Configuration (if using)
HITPAY_API_KEY=your-hitpay-api-key
HITPAY_SALT=your-hitpay-salt

# ==========================================
# ENVIRONMENT
# ==========================================

# Set to 'production' in Vercel environment variables
NODE_ENV=development

# ==========================================
# IMPORTANT NOTES FOR VERCEL DEPLOYMENT
# ==========================================
#
# 1. DO NOT commit .env file to git (it's in .gitignore)
# 2. Copy these variables to Vercel Dashboard:
#    - Go to: Project Settings > Environment Variables
#    - Add each variable for Production/Preview environments
# 3. DATABASE_URL is REQUIRED for Vercel (localhost won't work)
# 4. Generate new NEXTAUTH_SECRET for production (different from dev)
# 5. Update NEXTAUTH_URL to your Vercel deployment URL
#
